-----------------------------------------------------------
-- Comment.nvim 플러그인 설정 (lazy.nvim 관리)
-----------------------------------------------------------
local comment_nvim = {
	"numToStr/Comment.nvim", -- 주석 토글 및 라인 주석, 블록 주석 기능 제공 플러그인
	lazy = false, -- 즉시 로드 (lazy 로딩 비활성화)
}

-----------------------------------------------------------
-- 플러그인 설정 함수
-----------------------------------------------------------
comment_nvim.config = function()
	-- Comment.nvim의 기본 설정 적용
	require("Comment").setup({
		-- 추가 옵션이 필요한 경우 이곳에서 설정할 수 있습니다.
		-- 예: mappings = { basic = "gc", extra = "gb" }
	})
end

-----------------------------------------------------------
-- 플러그인 반환
-----------------------------------------------------------
return comment_nvim
